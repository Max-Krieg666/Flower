#order_info
  -if order
    -if order.line_items.blank?
      .alert.alert-info
        В корзине
        = Russian.p(order.total_line_items, 'товар', 'товара', 'товаров')
        на сумму
        = number_to_currency(order.total_amount)